VITE_API_URL=http://localhost:3000/api
# Clave publica VAPID del backend (GET /push/vapid-public-key).
# Sin ella no se ofrece activar notificaciones, pero la app funciona igual.
VITE_VAPID_PUBLIC_KEY=
