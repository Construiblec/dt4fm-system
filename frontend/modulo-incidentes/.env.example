VITE_API_URL=http://localhost:3000/api

# Autorizaciones (Accesos → CAV) usa datos quemados en vez de llamar al
# backend: ni Hostaway para ese endpoint ni el control de acceso están
# integrados todavía.
VITE_CAV_MOCK=false
